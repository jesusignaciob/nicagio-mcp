{
  "name": "nicagio-mcp",
  "version": "1.0.0",
  "private": true,
  "description": "Custom MCP servers by Nicagio",
  "scripts": {
    "install:all": "cd chroma-mcp && npm install && cd ../minimax-mcp && npm install",
    "build": "cd minimax-mcp && npm run build"
  }
}
