/**
 * Video Generation Tool — wraps `mmx video generate`.
 *
 * Single Responsibility: Only handles video generation.
 */
import type { ToolDefinition } from './base-tool.js';
import type { CliExecutor } from '../cli/executor.js';
export declare function createVideoTool(executor: CliExecutor): ToolDefinition;
//# sourceMappingURL=video-tool.d.ts.map